# ربات چتر

ربات مدیریت گروه تلگرام با پایتون. کتابخانه: `python-telegram-bot==20.7`، دیتابیس: SQLite (فایل `chatr_bot.db` خودکار ساخته می‌شه).

## نصب و اجرا

```bash
pip install -r requirements.txt
export BOT_TOKEN="توکن ربات از @BotFather"
python bot.py
```

یا مستقیم توی خود فایل `bot.py` مقدار `BOT_TOKEN` رو عوض کن.

## قابلیت‌ها

**منوی ☰ ربات (تو پیوی، به جای دکمه‌های شیشه‌ای):**
- `/start` — منوی اصلی
- `/addword` — یاد دادن کلمه جدید (فقط مالک گروه می‌تونه؛ اگه تو چند گروه مالک باشی، اول گروه رو انتخاب می‌کنی)
- `/mywords` — دیدن کلمات ساخته‌شده + ویرایش هرکدوم (تغییر جواب / اضافه کردن جواب رندوم دیگر — این هم فقط برای مالک)
- `/admin` — پنل مدیریت گروه (باید داخل خود گروه فرستاده بشه)
- `/cancel` — لغو عملیات جاری

همین دستورها داخل خود گروه هم کار می‌کنن (نیازی به پیوی نیست).

**پنل ادمین (`/admin` داخل گروه، فقط برای ادمین‌های واقعی گروه):**
- قفل / باز کردن گروه
- دادن لقب به عضو (باید روی پیام عضو ریپلای بشه)
- ثبت اخطار / حذف اخطار (حداکثر ۳ اخطار → حذف خودکار از گروه)
- کلمات ممنوعه (افزودن/حذف، حذف خودکار پیام + اخطار خودکار وقتی استفاده بشه)
- حذف پیام‌ها (حذف کل پیام‌های ردیابی‌شده یا تعداد دلخواه)
- افراد ویژه (معاف از حذف پیام به‌خاطر کلمات ممنوعه + امکان تعریف لحن مخصوص)
- تنظیمات گروه (فوروارد مجاز/ممنوع، محافظت اسپم به عنوان سوییچ ساده)

**دستورهای سریع (فقط ادمین‌ها، باید روی پیام همون عضو ریپلای بشه):**
- `بن` یا `صیکتیر` → همون لحظه از گروه بن می‌شه
- `اخطار` یا `اخطار <دلیل>` → یه اخطار سریع ثبت می‌کنه (دقیقاً با همون منطق و همون ۳ تا اخطارِ پنل ادمین)
- `سکوت <عدد>` (مثلاً `سکوت 5` یا `سکوت 100`) → طرف به همون تعداد دقیقه سکوت (میوت) می‌شه

## محدودیت مالک برای یاد دادن کلمه

فقط **مالک واقعی گروه** (Owner در تلگرام، نه هر ادمینی) می‌تونه کلمه جدید یاد بده یا جواب کلمات موجود رو ویرایش کنه. اعضای دیگه (حتی ادمین‌ها) این اجازه رو ندارن، تا لیست کلمات هر گروه واقعاً متعلق به همون مالک بمونه.

## نکات مهم

- ربات باید در گروه **ادمین** باشه و دسترسی‌های "حذف پیام"، "بن/میوت کردن کاربر"، "تغییر تنظیمات گروه" و "افزودن ادمین جدید" (برای قابلیت لقب) رو داشته باشه.
- برای اینکه یاد دادن کلمه از پیوی هم کار کنه، ربات باید حداقل یک بار همون کاربر رو داخل گروه دیده باشه (مثلاً یه پیام عادی، یا زدن `/start`/`/admin` تو گروه) تا گروه تو لیست گروه‌های اون کاربر ثبت بشه.
- قابلیت "حذف کل پیام‌های گروه" فقط شامل پیام‌هایی می‌شه که ربات از لحظه اضافه شدنش دیده و آی‌دیشون رو ذخیره کرده؛ تلگرام اجازه نمی‌ده به تاریخچه‌ی قبل از آن دسترسی داشته باشیم.
- محافظت اسپم و تنظیمات گروه فعلا به صورت سوییچ ساده (روشن/خاموش) پیاده شده و می‌تونه بعدا با قوانین دقیق‌تر (مثل محدودیت تعداد پیام در دقیقه) تکمیل بشه.
- برای افزودن ادمین دستی (بدون نیاز به ادمین بودن واقعی در تلگرام) می‌تونی مستقیم توی دیتابیس با تابع `add_manual_admin(chat_id, user_id)` این کار رو انجام بدی.
